meta {
  name: Get Profile
  type: http
  seq: 1
}

get {
  url: {{base_url}}/users/profile
  body: json
  auth: inherit
}

headers {
  Cookie: {{sessionCookie}}
}

settings {
  encodeUrl: true
  timeout: 0
}
